#linux #shell 

```bash
$ scp ./file.txt root@192.168.1.10:/tmp/
# ローカルからリモートへファイルを移動する
$ scp root@192.168.1.10:/tmp/file.txt ~/
# リモートからローカルへファイルを移動する
$ scp -r -3 ec2-user@192.168.1.10:/tmp/ root@192.168.1.10:/tmp/
# サーバー同士で通信を行う場合、互いが通信できないなら-3でローカルを中継させる
```